# Claude Code project instructions

@AGENTS.md

Before any UI or UX change, read and follow `AGENTS.md`, `.impeccable.md` and the relevant audit file. The rules in `AGENTS.md` are mandatory; do not bypass them with page-specific CSS, `!important`, arbitrary spacing, substitute icons or unverified screenshots.
